Drop project thumbnail images here (recommended: 1280x720 JPG/PNG, under ~300KB).

Then reference them in a project's front matter:

```yaml
thumbnail: /assets/images/projects/your-image.jpg
```

If a project has no `thumbnail` set, its card falls back to a simple colored
initial tile automatically — so this is optional, not required.
