Drop your résumé PDF here as `resume.pdf` (this exact filename matches the
`author.resume` path in `_config.yml`). The "Résumé" link in the site nav and
the link at the bottom of the About page both point here.
