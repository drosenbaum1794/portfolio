# Jekyll build output
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Ruby / Bundler
.bundle/
vendor/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp

# Claude Code local settings
.claude/

# Private drafts. This repo is PUBLIC, so anything not cleared for
# publication lives here and never gets committed.
_drafts-private/

# Full-size photo originals. Kept locally as the source; the site ships
# the optimized copies that sit alongside them.
assets/images/_originals/
